<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVVMLab"
        tools:targetApi="31">
        <activity
            android:name=".search.view.SearchActivity"
            android:exported="false"
            android:label="@string/title_activity_search"
            android:theme="@style/Theme.MVVMLab" />
        <activity
            android:name=".favourites.view.FavouritesActivity"
            android:exported="false"
            android:label="@string/title_activity_favourites"
            android:theme="@style/Theme.MVVMLab" />
        <activity
            android:name=".allProducts.view.AllProductsActivity"
            android:exported="false"
            android:label="@string/title_activity_all_products"
            android:theme="@style/Theme.MVVMLab" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MVVMLab">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>